@model IEnumerable<ASIGNMENT_FPOLY.Models.User>

<table class="table table-hover e-commerce-table dataTable no-footer">
    <thead>
        <tr role="row">
            <th class="sorting" tabindex="0" style="width: 35.325px;">
                <div class="checkbox">
                    <input id="checkAll" type="checkbox">
                    <label for="checkAll" class="m-b-0"></label>
                </div>
            </th>
            <th class="sorting">STT</th>
            <th class="sorting">UserName</th>
            <th class="sorting">Pass</th>
            <th class="sorting">Status</th>
            <th class="sorting"></th>
        </tr>
    </thead>
    <tbody>
        @{
            int Stt = 1;
        }
        @if (Model != null)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td class="">
                        <div class="checkbox">
                            <input type="checkbox">
                            <label></label>
                        </div>
                    </td>
                    <td>
                        @Stt
                    </td>
                    <td>@item.UserName</td>
                    <td>@item.PassWord</td>
                    <td class="sorting_1">
                        <div class="d-flex align-items-center">
                            <div class="badge badge-success badge-dot m-r-10"></div>
                            @{
                                string action;
                                action = @item.Stustus == 0 ? "Out Stock" : "In Stock";
                            }
                            <div>@action</div>
                        </div>
                    </td>
                    <td class="text-right">
                        <a class="btn btn-icon btn-hover btn-sm btn-rounded pull-right" asp-action="Edit" asp-route-id="@item.IdUser">
                            <i class="anticon anticon-edit"></i>
                        </a>
                        <a class="btn btn-icon btn-hover btn-sm btn-rounded" asp-action="Delete" asp-route-id="@item.IdUser">
                            <i class="anticon anticon-delete"></i>
                        </a>
                    </td>
                </tr>
                Stt++;
            }
        }
    </tbody>
</table>

